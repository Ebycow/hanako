# Discord音声Bot コマンド追加手順書

このドキュメントでは、Discord音声Botプロジェクトに新しいコマンドを追加する完全な手順を説明します。

## アーキテクチャ概要

このプロジェクトはDomain-Driven Design (DDD) パターンを採用し、以下の主要コンポーネントで構成されています：

- **コマンドパターン**: 各コマンドは独立したクラス
- **ファクトリーパターン**: Commandoがコマンドを解決
- **エンティティパターン**: CommandInputでデータ管理
- **サービス層**: MessageServiceが処理を統括

## 新規コマンド追加手順

### 1. コマンドクラスの実装

`src/domain/model/commands/` ディレクトリに新しいコマンドファイルを作成します。

```javascript
// 例: src/domain/model/commands/your_new_command.js
/**
 * 新しいコマンドの実装例
 */
class YourNewCommand {
    constructor(hanako) {
        this.hanako = hanako; // Hanakoモデルへの参照（必要に応じて）
    }

    /**
     * コマンドタイプの定義
     * @returns {string} コマンドタイプ
     */
    get type() {
        return 'your-new-command';
    }

    /**
     * コマンド名の別名リスト
     * @returns {string[]} コマンドの別名配列
     */
    static get names() {
        return ['新コマンド', 'newcmd', 'nc'];
    }

    /**
     * 実行に必要な権限（誰でも使えるコマンドなら省略）
     * 'manageGuild'（サーバー管理） または 'moderateMembers'（メンバーをタイムアウト）
     * スラッシュコマンドでは使える人の初期値になり、テキストで実行されたときはこちらで確認されます
     * @returns {PermissionName}
     */
    static get requiredPermission() {
        return 'manageGuild';
    }

    /**
     * スラッシュコマンドの定義（スラッシュコマンドにしない場合は省略）
     * オプション名がそのまま名前付きの引数（input.args）の名前になります
     * @returns {SlashCommandDefinition}
     */
    static get slash() {
        return {
            name: 'yournewcmd',
            description: '新しいコマンドの説明',
            options: [
                // type: 'string' | 'integer' | 'boolean' | 'user' | 'attachment'
                { type: 'string', name: 'parameter', description: 'パラメータの説明', required: true },
            ],
        };
    }

    /**
     * テキスト（>newcmd 引数）で入力された引数を名前付きの引数に変換（引数を取らない場合は省略）
     * 形式が間違っているときは、テキストでの使い方を案内するレスポンスを返します
     * @param {CommandInput} input - コマンド入力情報（input.argc / input.argv を読めるのはここだけ）
     * @returns {{args: object}|{response: Response}}
     */
    static parseText(input) {
        if (input.argc !== 1) {
            return { response: input.newChatResponse('引数が必要です 例:`@hanako 新コマンド 値`', 'error') };
        }
        return { args: { parameter: input.argv[0] } };
    }

    /**
     * メインの処理ロジック
     * テキストでもスラッシュコマンドでも、同じ名前付きの引数で呼ばれます
     * @param {CommandInput} input - コマンド入力情報
     * @returns {Response} レスポンス
     */
    process(input) {
        // 利用可能な情報:
        // input.args - 名前付きの引数（parseText または スラッシュコマンドのオプションから作られる）
        // input.source - 'text' か 'slash'
        // input.serverId - DiscordサーバーID
        // input.channelId - DiscordチャンネルID
        // input.userId - DiscordユーザーID
        // input.voiceChannelId - ボイスチャンネルID
        //
        // 引数の型:
        // 'string' は標準化済みの文字列（空白を含むこともある）
        // 'user' は { id, name }
        // 'attachment' は input.args.attachments 配列に { name, url } でまとめられる

        const argument = input.args.parameter;

        // 処理実装
        const result = this.executeLogic(argument);

        // レスポンスの返却
        return input.newChatResponse(`コマンド実行結果: ${result}`);
    }

    /**
     * コマンド固有のロジック
     * @private
     */
    executeLogic(argument) {
        // 実際の処理を実装
        return `処理完了: ${argument}`;
    }
}

module.exports = YourNewCommand;
```

### 2. コマンドの登録

`src/domain/model/commands/index.js` に新しいコマンドを追加します：

```javascript
module.exports = {
    ask: require('./ask_command'),
    join: require('./join_command'),
    leave: require('./leave_command'),
    // ... 既存のコマンド
    'your-new-command': require('./your_new_command'), // 追加
};
```

### 3. スラッシュコマンドとして登録（オプション）

スラッシュコマンドの定義は、手順1のようにコマンドクラスの `static slash` に書きます。
`deploy-commands.js` は全コマンドの `static slash` を集めて登録するだけなので、編集する必要はありません。
定義を追加・変更したら `node deploy-commands.js` で登録し直してください。

### 4. アクション実装（状態変更が必要な場合）

コマンドが状態変更を伴う場合は、`src/domain/entity/actions/` にアクションクラスを作成します：

```javascript
// src/domain/entity/actions/your_action.js
/**
 * カスタムアクション
 */
class YourAction {
    constructor(data) {
        this.data = data;
        this.timestamp = new Date();
    }

    get type() {
        return 'your-action';
    }

    // アクション固有のプロパティ
    get actionData() {
        return this.data;
    }
}

module.exports = YourAction;
```

`src/domain/entity/actions/index.js` に登録：

```javascript
module.exports = {
    // 既存のアクション...
    YourAction: require('./your_action'),
};
```

### 5. カスタムレスポンス実装（必要な場合）

特別なレスポンスが必要な場合は、`src/domain/entity/responses/` にレスポンスクラスを作成します：

```javascript
// src/domain/entity/responses/your_response.js
class YourResponse {
    constructor(data) {
        this.data = data;
    }

    get type() {
        return 'your-response';
    }
}
```

### 6. リポジトリ実装（データ永続化が必要な場合）

データベース操作が必要な場合：

1. `src/domain/repo/` にインターフェース定義を作成
2. `src/infra/nedb/` に実装クラスを作成
3. `src/core/injector.js` に依存注入の設定を追加

```javascript
// src/domain/repo/i_your_data_repo.js
class IYourDataRepo {
    async save(data) {
        throw new Error('Not implemented');
    }
    
    async findById(id) {
        throw new Error('Not implemented');
    }
}
```

### 7. テストの実装

`test/unit_specs/` にユニットテストを作成します：

```javascript
// test/unit_specs/domain_model_commands_your_new_command_unit_spec.js
require('chai').should();
const YourNewCommand = require('../../src/domain/model/commands/your_new_command');
const { basicHanako, commandInputBlueprint, processText } = require('../helpers/blueprints');

describe('YourNewCommand', () => {
    let command;

    beforeEach(() => {
        command = new YourNewCommand(basicHanako());
    });

    describe('process', () => {
        // テキストで入力されたときと同じ手順（parseText → process）で実行する
        it('テキストの引数で実行できる', () => {
            const input = commandInputBlueprint({ argc: 1, argv: ['test'] });
            const response = processText(command, input);

            response.type.should.equal('chat');
            response.content.should.include('test');
        });

        it('テキストの引数がないと形式エラー', () => {
            const input = commandInputBlueprint({ argc: 0, argv: [] });
            const response = processText(command, input);

            response.code.should.equal('error');
        });

        // スラッシュコマンドでは名前付きの引数で process が直接呼ばれる
        it('名前付きの引数で実行できる', () => {
            const input = commandInputBlueprint().withArgs({ parameter: 'スペース を含む値' });
            const response = command.process(input);

            response.content.should.include('スペース を含む値');
        });
    });
});
```

`commandInputBlueprint` と `processText` は `test/helpers/blueprints.js` にあります。

### 8. デプロイと動作確認

#### スラッシュコマンドのデプロイ
```bash
node deploy-commands.js
```

#### アプリケーションの起動
```bash
npm start
```

#### 動作確認
1. **テキストコマンド**: `@hanako 新コマンド` または `>新コマンド`
2. **スラッシュコマンド**: `/yournewcmd`
3. **引数付きコマンド**: `@hanako 新コマンド 引数1 引数2`

## レスポンスタイプ

コマンドは以下のレスポンスタイプを返すことができます：

- **ChatResponse**: テキストレスポンス
- **ActionResponse**: 状態変更アクション
- **VoiceResponse**: 音声レスポンス
- **SilentResponse**: 無応答

```javascript
// 使用例
return input.newChatResponse('テキストメッセージ');
return input.newActionResponse(new YourAction(data));
return input.newSilentResponse();
```

## 既存コマンドの参考例

- **`join_command.js`** - 基本的なコマンド実装
- **`word_create_command.js`** - 引数処理とデータ操作
- **`foley_create_command.js`** - ファイル処理
- **`help_command.js`** - 複雑なレスポンス生成

## 注意点

1. **命名規則**: ファイル名は `snake_case`、クラス名は `PascalCase`
2. **エラーハンドリング**: 適切な例外処理を実装
3. **テスト**: 新機能には必ずテストを追加
4. **ドキュメント**: JSDocコメントを適切に記述
5. **依存関係**: 必要最小限の依存関係に留める

## トラブルシューティング

### よくある問題

1. **コマンドが認識されない**
   - `index.js` への登録を確認
   - `names` プロパティの設定を確認

2. **スラッシュコマンドが表示されない**
   - `deploy-commands.js` の実行を確認
   - Discord Developer Portalでの設定を確認

3. **テストが失敗する**
   - モックオブジェクトの設定を確認
   - 非同期処理の適切な処理を確認

この手順書に従って実装することで、既存のアーキテクチャに適合した新しいコマンドを確実に追加できます。